/**Hacer un programa en C tal que se imprima en pantalla
 * 128 veces hola mundo usando una sola linea con printf() sin ciclos
 * */
#include <stdio.h>
#include <sys/types.h>
int a=0;
int main()
{
 fork(); 
 fork();
 fork();
 fork();
 fork();
 fork();
 fork();
 printf("Hola mundo %d",a++);
}